// Catalog request schemas are defined in the shared package and reused here.
export {
  createCategorySchema,
  updateCategorySchema,
  createExpertiseTagSchema,
  createLanguageSchema,
} from "../../shared/index.js";
