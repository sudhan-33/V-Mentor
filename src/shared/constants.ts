/** Platform-wide business constants. Promote to config/env if they become tunable. */

export const DEFAULT_CURRENCY = "INR";
export const DEFAULT_TIMEZONE = "Asia/Kolkata";
export const DEFAULT_SESSION_MINUTES = 60;

/** Platform commission taken from a mentor's gross session price. */
export const PLATFORM_FEE_PERCENT = 15;

/** Days an earning is held before it becomes withdrawable. */
export const EARNING_HOLDING_DAYS = 7;

export const PASSWORD_MIN_LENGTH = 8;
